# Pinned binary versions — SINGLE SOURCE OF TRUTH for a reproducible machine.
# Sourced by install-binaries.sh and referenced by SETUP.md. Bump deliberately,
# validate, commit (same discipline as lazy-lock.json / bootstrap-tools.sh).
NVIM_VERSION=0.12.4
FD_VERSION=10.4.2
LAZYGIT_VERSION=0.63.0
FZF_VERSION=0.74.0
NODE_VERSION=22.20.0
RIPGREP_VERSION=14.1.1

# Terminal Nerd Font (installed manually per SETUP.md — GUI/font-cache step).
NERD_FONT=JetBrainsMono
NERD_FONT_VERSION=3.4.0
